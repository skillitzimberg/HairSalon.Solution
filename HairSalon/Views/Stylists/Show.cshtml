@{
  Layout = "_Layout";
}
<div class="container index">
  <nav class="navbar navbar-inverse navbar-fixed-top">
    <span class="logo"><a href="/">HairSalon</a></span>
    <ul>
      <li><a href="/stylists">View All Stylists</a></li>
      <li><a href='/stylists/@Model["stylist"].GetId()/clients/new'>Add New Client</a></li>
    </ul>
  </nav>
  <div class="content">
    <h3>@Model["stylist"].GetFullName()</h3>

    <h3>Clients: </h3>

      @foreach (var client in @Model["clients"])
      {
        <h4><a href='/stylists/@Model["stylist"].GetId()/clients/@client.GetId()'>@client.GetClientName()</a></h4>
      }
  </div>
</div>
